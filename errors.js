/**
 * ValMerge error handler
 */

module.exports = {
	invalidConfig: 'Invalid config file',
	missingEmail: 'Email field is missing',
	missingMessage: 'Message Template is missing',
	missingData: 'config.query or config.query.db is missing',
	missingMailServer: 'One of config.mailServer.* info is missing',
}
